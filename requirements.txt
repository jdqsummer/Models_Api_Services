# Python依赖包列表 (兼容Python 3.13)
Flask==2.3.3
Flask-CORS==4.0.0
dashscope>=1.14.0
requests==2.31.0
tencentcloud-sdk-python>=3.0.1441
openai==1.55.3
gunicorn==21.2.0
python-dotenv==1.0.1

# Python 3.13兼容性说明:
# 所有列出的包都经过测试，兼容Python 3.13
# 如果遇到兼容性问题，可以尝试更新到最新版本:
# pip install --upgrade flask flask-cors dashscope requests tencentcloud-sdk-python openai gunicorn python-dotenv

# 安装命令: pip install -r requirements.txt
# 或者使用虚拟环境: python -m venv venv && venv\Scripts\activate && pip install -r requirements.txt